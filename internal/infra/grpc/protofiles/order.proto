syntax = "proto3";
package pb;
option go_package = "internal/infra/grpc/pb";

message blank{}

message CreateOrderRequest {
  string id = 1;
  float price = 2;
  float tax = 3;
}

message CreateOrderResponse {
  string id = 1;
  float price = 2;
  float tax = 3;
  float final_price = 4;
}

message OrderResponse {
  string id = 1;
  float price = 2;
  float tax = 3;
  float final_price = 4;
}

message ListOrderResponse {
  repeated OrderResponse orders = 1;
}

service OrderService {
  rpc CreateOrder(CreateOrderRequest) returns (CreateOrderResponse);
  rpc ListOrder(blank) returns (ListOrderResponse);
}

//Para atualizar o proto
//protoc --go_out=. --go-grpc_out=. internal/infra/grpc/protofiles/order.proto

//Caso precise: Atualizando o grpc para a ultima versão no go.mod:
//go get -u google.golang.org/grpc@latest
//go mod tidy

//Apos isto será necessario rodar o protoc novamente